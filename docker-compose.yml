version: '3'
services:
  voters:
    image: larshaalck/voters:latest
    restart: unless-stopped
    volumes:
      - ./db:/opt/voters/db
      - ./Rocket.toml:/opt/voters/Rocket.toml
    ports:
      - "8000:8000"
