version: '3'
services:
  prank:
    image: larshaalck/paper-rank:latest
    restart: unless-stopped
    volumes:
      - ./db:/opt/voters/db
      - ./Rocket.toml:/opt/voters/Rocket.toml
    env_file:
    - .env
    ports:
      - "8000:8000"
